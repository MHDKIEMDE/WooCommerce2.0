services:

  # ── PHP-FPM — Laravel ────────────────────────────────────────────────────────
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: shop-app
    restart: unless-stopped
    env_file: /var/www/shop/.env
    volumes:
      - /var/www/shop/.env:/var/www/html/.env:ro
      - storage:/var/www/html/storage
      - sqlite:/var/www/html/database
      - public:/var/www/html/public
    depends_on:
      - redis
    networks:
      - shop-network

  # ── Nginx ─────────────────────────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: shop-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - storage:/var/www/html/storage:ro
      - public:/var/www/html/public:ro
    depends_on:
      - app
    networks:
      - shop-network

  # ── Redis (cache + queues) ────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: shop-redis
    restart: unless-stopped
    volumes:
      - redis-data:/data
    networks:
      - shop-network

  # ── Queue Worker ──────────────────────────────────────────────────────────────
  queue:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: shop-queue
    restart: unless-stopped
    env_file: /var/www/shop/.env
    command: php artisan queue:work --sleep=3 --tries=3 --max-time=3600
    volumes:
      - /var/www/shop/.env:/var/www/html/.env:ro
      - storage:/var/www/html/storage
      - sqlite:/var/www/html/database
    depends_on:
      - redis
    networks:
      - shop-network

  # ── Scheduler (cron Laravel) ──────────────────────────────────────────────────
  scheduler:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: shop-scheduler
    restart: unless-stopped
    env_file: /var/www/shop/.env
    command: sh -c "while true; do php artisan schedule:run --no-interaction; sleep 60; done"
    volumes:
      - /var/www/shop/.env:/var/www/html/.env:ro
      - storage:/var/www/html/storage
      - sqlite:/var/www/html/database
    depends_on:
      - redis
    networks:
      - shop-network

volumes:
  storage:
  sqlite:
  public:
  redis-data:

networks:
  shop-network:
    driver: bridge
